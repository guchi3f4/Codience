<% users.each do |user| %>
  <div class='col-lg-4 col-md-6 mb-3 px-2'>
    <%= render 'users/user_info', user: user %>
  </div>
<% end %>

<div class="d-flex justify-content-center py-2"><%= paginate users, remote: true %></div>